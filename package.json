{
  "name": "socialgraph-fm-governance",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "build:web": "npm --prefix apps/web run build",
    "bundle:web": "npm run typecheck:web && npm run build:web && python scripts/build-web-bundle.py",
    "check:web-bundle": "npm run typecheck:web && npm run build:web && python scripts/build-web-bundle.py --check",
    "test:web": "npm --prefix apps/web test",
    "typecheck:web": "npm --prefix apps/web run typecheck",
    "test:e2e": "npm --prefix apps/web run test:e2e:offline"
  }
}
