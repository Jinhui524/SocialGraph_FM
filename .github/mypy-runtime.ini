[mypy]
python_version = 3.12

[mypy-socialgraph_fm_runtime.processes,socialgraph_fm_runtime.subprocess_control]
# POSIX typeshed omits Win32 process constants and WinDLL. Runtime os.name
# guards are still enforced; this is a CI-only platform-stub exception.
disable_error_code = attr-defined
